import Image from "next/image";
import { MapPin } from "lucide-react";
import { Button } from "@/components/ui/Button";
import { buildWhatsAppUrl } from "@/lib/whatsapp";

export function Hero() { return <section id="inicio" className="mx-auto grid w-[min(100%,96rem)] lg:min-h-[calc(100svh-5rem)] lg:grid-cols-[42%_58%]"><div className="flex flex-col justify-center px-4 py-16 md:px-10 lg:px-[max(2.5rem,calc((100vw-86rem)/2))] lg:pr-12"><p className="mb-6 text-xs font-bold uppercase tracking-[.24em] text-[var(--brand-light)]">Coworking em Manaus</p><h1 className="font-display max-w-[11ch] text-[clamp(3.6rem,6vw,6.8rem)] leading-[.9] tracking-[-.035em]">Trabalhe melhor em Manaus.</h1><p className="font-display mt-7 text-2xl text-[var(--brand)] md:text-3xl">Seu espaço. Sua comunidade. Seu ritmo.</p><p className="mt-6 max-w-lg leading-7 text-[var(--muted)]">Coworking para quem busca foco, conexão e estrutura.</p><div className="mt-9 flex flex-col gap-3 sm:flex-row"><Button href={buildWhatsAppUrl()}>Agendar visita</Button><Button href="#espacos" variant="outline">Explorar espaços</Button></div><p className="mt-12 flex items-center gap-2 text-sm"><MapPin size={18} className="text-[var(--brand)]"/>No coração de Manaus.</p></div><div className="relative min-h-[30rem] overflow-hidden lg:min-h-full"><Image src="/images/hero.jpg" alt="Interior contemporâneo do coworking NorteHub" fill priority sizes="(max-width: 1024px) 100vw, 58vw" className="object-cover transition-transform duration-700 hover:scale-[1.015]"/></div></section>; }
