import Image from "next/image";
import { ArrowUpRight } from "lucide-react";
import { spaces } from "@/data/content";
export function Spaces() { return <section id="espacos" className="section-shell" aria-labelledby="spaces-title"><div className="mb-10 flex items-end justify-between"><div><p className="mb-3 text-xs font-bold uppercase tracking-[.2em] text-[var(--brand-light)]">Nossos espaços</p><h2 id="spaces-title" className="section-title">Arquitetura para o seu ritmo.</h2></div><ArrowUpRight className="hidden text-[var(--brand)] md:block" size={36}/></div><div className="grid auto-rows-[17rem] gap-4 md:grid-cols-12 md:auto-rows-[20rem]">{spaces.map((space,index)=><article key={space.id} className={`group relative overflow-hidden ${index===0?"md:col-span-7 md:row-span-2":index===1?"md:col-span-5":"md:col-span-5"}`}><Image src={space.image} alt={space.name} fill sizes="(max-width:768px) 100vw, 60vw" className="object-cover transition-transform duration-700 group-hover:scale-[1.025]"/><div className="absolute inset-x-0 bottom-0 flex items-end justify-between bg-gradient-to-t from-black/70 to-transparent p-6 pt-20 text-white"><div><h3 className="font-display text-3xl">{space.name}</h3><p className="mt-1 text-xs opacity-80">{space.price}</p></div><ArrowUpRight/></div></article>)}</div></section>; }
