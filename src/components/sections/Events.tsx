import Image from "next/image";
import { ArrowRight } from "lucide-react";
import { events } from "@/data/content";
export function Events() { return <section id="eventos" className="bg-[var(--brand)] text-white"><div className="section-shell"><div className="flex items-end justify-between gap-4"><h2 className="section-title">Próximos eventos</h2><a href="#" className="focus-ring flex items-center gap-2 text-sm font-bold">Ver todos <ArrowRight size={16}/></a></div><div className="mt-12 grid gap-px bg-white/20 sm:grid-cols-2 lg:grid-cols-4">{events.map(event=><article key={event.name} className="group bg-[var(--brand)] pb-6"><div className="relative aspect-[4/3] overflow-hidden"><Image src={event.image} alt="" fill sizes="(max-width:640px) 100vw, 25vw" className="object-cover opacity-80 grayscale-[25%] transition group-hover:scale-[1.02] group-hover:opacity-100"/><span className="absolute left-4 top-4 bg-[var(--background)] px-3 py-2 text-center text-[var(--foreground)]"><b className="block text-lg leading-none">{event.day}</b><small className="text-[9px] font-bold">{event.month}</small></span></div><div className="px-5 pt-5"><p className="text-[10px] font-bold uppercase tracking-[.18em] text-white/60">{event.category}</p><h3 className="font-display mt-2 text-2xl">{event.name}</h3></div></article>)}</div></div></section>; }
