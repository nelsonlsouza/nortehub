import { Check } from "lucide-react";
import { plans } from "@/data/content";
import { Button } from "@/components/ui/Button";
import { buildWhatsAppUrl } from "@/lib/whatsapp";
export function Plans() { return <section id="planos" className="border-y border-[var(--border)] bg-[var(--surface)]"><div className="section-shell"><h2 className="section-title">Planos que se adaptam a você</h2><div className="mt-12 grid gap-px overflow-hidden border border-[var(--border)] bg-[var(--border)] lg:grid-cols-3">{plans.map(plan=><article className={`relative flex flex-col bg-[var(--background)] p-7 md:p-10 ${plan.featured?"lg:-my-3 lg:border-y-[3px] lg:border-[var(--brand)] lg:py-[calc(2.5rem+9px)]":""}`} key={plan.name}>{plan.featured&&<span className="mb-5 w-fit bg-[var(--brand)] px-3 py-1 text-[10px] font-bold uppercase tracking-wider text-white">Mais escolhido</span>}<h3 className="font-display text-3xl">{plan.name}</h3><p className="mt-3 min-h-12 text-sm leading-6 text-[var(--muted)]">{plan.description}</p><p className="mt-7"><span className="font-display text-4xl">{plan.price}</span><span className="text-sm text-[var(--muted)]"> {plan.unit}</span></p><ul className="my-8 grid gap-3 text-sm">{plan.features.map(feature=><li className="flex items-center gap-3" key={feature}><Check size={16} className="text-[var(--brand)]"/>{feature}</li>)}</ul><Button className="mt-auto" variant={plan.featured?"primary":"outline"} href={buildWhatsAppUrl(`Olá! Gostaria de saber mais sobre o plano ${plan.name}.`)}>Quero este plano</Button></article>)}</div></div></section>; }
